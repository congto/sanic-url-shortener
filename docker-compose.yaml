version: '2'
services:
    nginx:
        image: namptit307/url-shortener-nginx
        ports:
            - "8888:80"
        links:
            - app
    app:
        image: namptit307/url-shortener-app
        expose:
            - "8000"
        links:
            - postgres
        env_file:
            - ./.env
        depends_on:
            - postgres

    postgres:
        image: namptit307/url-shortener-db
        env_file:
            - ./.env
        volumes:
            - ./pg:/var/lib/postgres/data/
        expose:
            - "5432"
